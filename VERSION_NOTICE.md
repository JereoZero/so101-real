# ⚠️ 版本说明

> **如果你是来看 LeRobot v0.6.0 的流程和代码，请直接前往 [`lerobot-0.6/`](./lerobot-0.6/) 目录。**

## 目录对应版本

| 目录 | LeRobot 版本 | 说明 |
|------|-------------|------|
| [`lerobot-0.6/`](./lerobot-0.6/) | **v0.6.0（最新）** | 代码、文档、脚本，活跃开发中 |
| [`lerobot-0.5/`](./lerobot-0.5/) | v0.5.1（老版本） | 踩坑记录 + 项目流程，已完成 |

## 为什么要区分？

LeRobot v0.6.0 相比 v0.5.1 有较多 breaking change：

- 真机推理命令从 `lerobot-eval` 变为 `lerobot-rollout`
- 多数据集训练（`repo_id` 列表）已被禁用，需物理合并
- 视频解码后端有兼容性问题（需用 `pyav` 替代 `torchcodec`）
- 录制时 `repo_id` 自动追加时间戳
- 新增 DAgger 人机回环、RTC 实时推理等特性

**老版本的内容仍有很大参考价值**：硬件连接、驱动安装、摄像头配置、LeRobot 源码修改等通用问题是跨版本共用的，v0.6 的很多设置也是在此基础上搭建的。老版本的踩坑记录和流程更详细，建议对照阅读。但涉及 LeRobot 命令和参数的部分请以 `lerobot-0.6/` 中的文档为准。
